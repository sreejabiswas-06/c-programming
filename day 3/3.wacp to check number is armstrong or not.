#include <stdio.h>
int main()
{
int n;
printf("enter any number ");
scanf ("%d",n);
int r,sum=o,temp=n;
while(n>0)
{
r=n%10;
sum=sum +(r*r*r);
n=n/10;
}
if(temp ==sum)
{
printf("It is an armstrong number ");
}
else 
{
printf ("It is not an armstrong number );
}
return 0;
}
