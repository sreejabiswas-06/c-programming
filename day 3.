write a c program to count the number of digits.
  #include<stdio.h>
  int main()
{
  int n,count = 0;
  printf ("enter any num to count the num  of digits");
  scanf("%d".&n);
  while(n;=0)
  {
    n=n/10;
    count++;
  }
  printf("the number of digit are %d",count);
  return 0;
}
